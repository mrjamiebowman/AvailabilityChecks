apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "availabilitychecks.fullname" . }}-test-connection"
  labels:
    {{- include "availabilitychecks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "availabilitychecks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
