apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mrjb-availabilitychecks.fullname" . }}-test-connection"
  labels:
    {{- include "mrjb-availabilitychecks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mrjb-availabilitychecks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
